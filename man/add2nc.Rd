% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add2nc.R
\name{add2nc}
\alias{add2nc}
\title{Add timestamp-aligned vectors from a data frame into an existing NetCDF file}
\usage{
add2nc(
  nc_path,
  source_df,
  source_time_col = "TIMESTAMP",
  vars_to_add,
  new_var_names = NULL,
  units = "unknown",
  longname = NULL,
  missval = -9999,
  nc_time_var = "time",
  tz = "UTC",
  source_tz = tz,
  time_offset_minutes = 6,
  time_offset_direction = c("subtract", "add", "none"),
  new_file = TRUE,
  output_nc_path = NULL
)
}
\arguments{
\item{nc_path}{Character scalar. Path to the existing NetCDF file to modify.}

\item{source_df}{Data frame containing a timestamp column and variables to write.}

\item{source_time_col}{Character scalar. Column name in \code{source_df} containing
timestamps. Default \code{"TIMESTAMP"}.}

\item{vars_to_add}{Character vector. Source column names to import from \code{source_df}.}

\item{new_var_names}{Character vector or \code{NULL}. Output variable names in the NetCDF
file. If \code{NULL}, defaults to \code{paste0(vars_to_add, "_profile")}.}

\item{units}{Character scalar or character vector. Units string(s) assigned to newly
created NetCDF variables. If length 1, the same units are used for all variables.
Ignored for variables that already exist in the file.}

\item{longname}{Character scalar, character vector, or \code{NULL}. Long name(s) assigned
to newly created NetCDF variables. If length 1, the same long name is used for all
variables. If \code{NULL}, defaults to
\code{paste(vars_to_add, "imported from source dataframe")}.
Ignored for variables that already exist in the file.}

\item{missval}{Numeric scalar. Missing-value code used when building full-length
output vectors. Default \code{-9999}.}

\item{nc_time_var}{Character scalar. Preferred NetCDF time axis name (e.g. \code{"time"}).
If not found, a time-like dimension (units containing \code{"since"}) is auto-detected.}

\item{tz}{Character scalar. Time zone used for NetCDF timestamp conversion and
matching. Default \code{"UTC"}.}

\item{source_tz}{Character scalar. Time zone used when parsing source timestamps.
Defaults to \code{tz}.}

\item{time_offset_minutes}{Numeric scalar. Fixed minute offset applied to source
timestamps before matching. Default \code{6}.}

\item{time_offset_direction}{Character scalar. One of \code{"subtract"}, \code{"add"},
\code{"none"}. Controls how \code{time_offset_minutes} is applied to source timestamps.
Default \code{"subtract"}.}

\item{new_file}{Logical scalar. If \code{TRUE}, a new NetCDF file is created for output.
If \code{FALSE}, the existing file at \code{nc_path} is modified in place. Default \code{TRUE}.}

\item{output_nc_path}{Character scalar or \code{NULL}. Path for the output NetCDF file.
Required if \code{new_file} is \code{TRUE}. If \code{NULL}, defaults to
\verb{<nc_path>_updated.nc}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per imported variable containing:
\describe{
\item{source_var}{Source column name in \code{source_df}.}
\item{target_var}{Target variable name written in the NetCDF file.}
\item{matched_rows}{Number of source rows matched to NetCDF timestamps.}
\item{nc_time_points}{Length of the NetCDF time axis.}
\item{resolved_time_name}{NetCDF time axis name that was used.}
\item{time_offset_minutes}{Minute offset value that was applied.}
\item{time_offset_direction}{Offset direction that was applied.}
\item{output_nc_path}{Path to the output NetCDF file.}
}
}
\description{
Imports one or more numeric vectors from \code{source_df} into \code{nc_path},
aligning source rows to the NetCDF time axis using exact timestamp matching.
Optionally shifts source timestamps by a fixed minute offset before matching
and creates target NetCDF variables if they do not already exist.
}
\details{
\strong{Process overview:}
\enumerate{
\item Validate required inputs (\code{source_time_col}, \code{vars_to_add}, naming lengths).
\item Open NetCDF in write mode and resolve the time axis (\code{nc_time_var} or auto-detect).
\item Convert NetCDF time to POSIXct using CF-compliant units/calendar.
\item Parse source timestamps and apply optional fixed offset.
\item Filter source rows to NetCDF time window, deduplicate timestamps (keep last),
then exact-match to NetCDF indices.
\item Add missing variables to the NetCDF file and write full-length aligned vectors.
\item Return a tibble with diagnostics on matching and settings used.
}
}
\examples{
\dontrun{
result <- add2nc(
  nc_path = "my_flux_file.nc",
  source_df = profile,
  source_time_col = "TIMESTAMP",
  vars_to_add = c("FC_Storage"),
  time_offset_minutes = 6,
  time_offset_direction = "subtract"
)
}

}
