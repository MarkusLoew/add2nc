% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_global_nc_attributes.R
\name{add_global_nc_attributes}
\alias{add_global_nc_attributes}
\title{Add or update global attributes in a NetCDF file}
\usage{
add_global_nc_attributes(
  nc_path,
  attributes,
  new_file = TRUE,
  output_nc_path = NULL
)
}
\arguments{
\item{nc_path}{Character scalar. Path to the existing NetCDF file.}

\item{attributes}{Data frame or named list of global attributes to add.
\itemize{
\item \strong{Data frame}: must have columns \code{name} and \code{value}.
\item \strong{Named list}: names become attribute names, values become attribute values.
}}

\item{new_file}{Logical scalar. If \code{TRUE}, a copy of \code{nc_path} is written to
\code{output_nc_path} and that copy is modified. If \code{FALSE}, \code{nc_path} is
modified in place. Default \code{TRUE}.}

\item{output_nc_path}{Character scalar or \code{NULL}. Destination path when
\code{new_file = TRUE}. If \code{NULL} the path is derived by appending \verb{_updated}
before the \code{.nc} extension of \code{nc_path}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per attribute containing:
\describe{
\item{attribute_name}{Name of the global attribute.}
\item{attribute_value}{Value written to the global attribute.}
\item{status}{\code{"added"} if the attribute was new; \code{"updated"} if it already
existed.}
\item{output_nc_path}{Path to the (possibly new) output NetCDF file.}
}
}
\description{
Adds or updates global metadata attributes in an existing NetCDF file.
Supports any site-level or dataset-level attributes supplied as a named
list or as a two-column data frame.
}
\details{
\strong{Process overview:}
\enumerate{
\item Validate and normalise the \code{attributes} argument into a name/value table.
\item Optionally copy the input NetCDF file to \code{output_nc_path}.
\item Open the NetCDF file in write mode.
\item Add or overwrite each global attribute with \code{\link[ncdf4:ncatt_put]{ncdf4::ncatt_put()}}.
\item Return a tibble summarising what was written.
}
}
\examples{
\dontrun{
# Data-frame interface
attrs_df <- data.frame(
  name  = c("longitude", "latitude", "site_pi", "contact"),
  value = c("144.5234", "-37.4321", "John Doe", "john@example.com")
)
result <- add_global_nc_attributes(
  nc_path        = "my_file.nc",
  attributes     = attrs_df,
  new_file       = TRUE,
  output_nc_path = "my_file_updated.nc"
)

# Named-list interface
attrs_list <- list(
  longitude = 144.5234,
  latitude  = -37.4321,
  site_pi   = "Jane Smith",
  contact   = "jane@example.com"
)
result <- add_global_nc_attributes(
  nc_path    = "my_file.nc",
  attributes = attrs_list
)
}

}
